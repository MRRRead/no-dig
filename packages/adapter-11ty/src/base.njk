<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>{{ title or 'NO-DIG Site' }}</title>
</head>
<body>
  <nav>
    <ul>
      {% for item in navigation %}
        <li>
          {% if item.path %}
            <a href="/{{ item.path | replace('.md', '') }}">{{ item.name | replace('.md', '') }}</a>
          {% else %}
            {{ item.name }}
          {% endif %}
          {% if item.children and item.children.length > 0 %}
            <ul>
              {% for child in item.children %}
                <li>
                  <a href="/{{ child.path | replace('.md', '') }}">{{ child.name | replace('.md', '') }}</a>
                </li>
              {% endfor %}
            </ul>
          {% endif %}
        </li>
      {% endfor %}
    </ul>
  </nav>
  <main>
    {{ content | safe }}
  </main>
</body>
</html>
