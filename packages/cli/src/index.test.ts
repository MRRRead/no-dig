const { execSync } = require('child_process');

describe('CLI Build Command', () => {
  it('runs no-dig build without error', () => {
    // TODO: This is a placeholder; actual CLI test will require bin script
    expect(true).toBe(true);
  });
});
